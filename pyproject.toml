[project]
name = "resume-generator"
version = "0.1.0"
requires-python = ">=3.14.2"

[tool.mypy]
# Use a sensible default and incrementally adopt strictness per-module.
ignore_missing_imports = false
warn_unused_configs = true

[tool.mypy-resume.repository]
strict = true
